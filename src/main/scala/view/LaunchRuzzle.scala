package view

import javafx.application.Application

/**
  * The LaunchRuzzle object that allows to start the game.
  */
object LaunchRuzzle extends App {
  Application.launch(classOf[LauchDashboard])
}
